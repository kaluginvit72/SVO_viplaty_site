# Docker / GHCR / VPS — куда смотреть

Полная инструкция по production-деплою (Docker, GitHub Actions, GHCR, Compose, Nginx, Certbot, лиды, откат) — в **[web/README.md](../web/README.md)**, раздел **«Деплой в production: Docker, GHCR, GitHub Actions, VPS, Nginx»**.

Пошагово: установка Docker на VPS, каталог, `.env`, `docker compose pull/up`, проверка health, firewall, обновления и вариант сборки без GHCR — подзаголовок **«Пошаговый деплой на VPS: контейнер и запуск»** в том же разделе.
